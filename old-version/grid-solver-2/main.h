#ifndef MAIN_H_
#define MAIN_H_

#include <sstream>
#include <string>
#include <stdio.h>
#include <iostream>
#include <math.h>

using namespace std;

#define NaN log(-1.0f)
#define QUAD(x) ((x)*(x))
#define CUBE(x) ((x)*(x)*(x))
struct COORD_int {
	int x, y;
};

#endif	// MAIN_H_
