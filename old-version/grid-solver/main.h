#ifndef _MAIN_H_
#define _MAIN_H_

#include<string>
#include<stdio.h>
#include <iostream>

using namespace std;

#define NaN log(-1.0f)
#define QUAD(x) ((x)*(x))
#define CUBE(x) ((x)*(x)*(x))
struct COORD_int {
	int x, y;
};

#endif